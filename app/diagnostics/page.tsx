import { AppShell } from '@/components/app-shell';
export default function Page(){return <AppShell title='diagnostics'><div className='card'>diagnostics module ready. Local-first mode enabled. Simulated runtime where noted.</div></AppShell>}
